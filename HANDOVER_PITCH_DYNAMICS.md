# 蝴蝶扑翼机器人俯仰动力学仿真 — 工作交接文档

> 撰写日期：2026-06-05  
> 当前分支：`feature/pitch-reversal`  
> 交接目的：供后续 AI / 开发者完整接手、继续迭代、反思与修正。  

---

## 1. 项目背景与目标

### 1.1 物理问题
用户正在开发一台**蝴蝶仿生扑翼微型飞行器（MAV）**，质量约 **20 g**。需要验证的核心问题是：

- **俯仰稳定性**：翅膀拍动产生的气动力能否在俯仰方向上形成自稳定（restoring moment）？
- **升力/推力平衡**：能否产生足够升力抵消重力，同时在上拍时产生前向推力？
- **同相 vs 反相拍动**：前后翅的相位差（baseline 同相 / anti-phase 180° / 90°）对稳定性和推进效率的影响。

### 1.2 用户的核心理论图像（必须严格遵守）
1. **固定安装角 40°**（可调整优化）：当蝴蝶机身水平（俯仰角 θ_p = 0）时，翅膀攻角约为 40°，攻角方向即俯仰方向。
2. **拍动方向约定**：翅膀**向上拍**（前缘上抬）角度为正，**向下拍**角度为负。
3. **上拍产生向前推力**（Fx > 0），下拍产生**向上升力**（Fz > 0）。
4. **俯仰恢复机制**：前倾时前后翅产生不同的力方向，形成恢复力矩使机身回正。
5. **大俯仰角可接受**：即使初始俯仰 30°+ 也可以，只要恢复力矩 + 升力能平衡重力即可。

### 1.3 当前机构关键参数
- **机构**：曲柄摇杆四连杆（`src/mechanism.py`）
- **基准参数**：`a = 7.92 mm`，`f = 15 Hz`，顺时针旋转（`cw`）
- **角度偏移**：`phi_offset_deg = -50.84°`，使拍动关于水平对称
- **拍动范围**：`φ ∈ [-22.2°, +22.2°]`（偏移后）
- **急回特性**：下拍快、上拍慢（顺时针旋转时）

---

## 2. 工作发展历程

### 2.1 版本演进

| 版本 | 文件名 | 状态 | 核心内容 | 问题与教训 |
|------|--------|------|----------|------------|
| v1 | `pitch_dynamics.py` | 遗留 | 初版准定常模型 | 力投影公式未严格验证 |
| v2 | `pitch_dynamics_v2.py` | 遗留 | 引入几何参数 | 面积单位 bug（`e-3` 误写，已在 v3 修正为 `e-6`） |
| v3 | `pitch_dynamics_v3.py` | 遗留 | 验证投影公式、分离上下拍攻角 | **对称攻角导致净负升力**（急回特性使上拍更快，负升力占优） |
| v4 | `pitch_dynamics_v4.py` | **废弃** | 尝试严格 3D 旋转矩阵 | **双重投影 bug**：先在 wing  frame 用 `sin(psi)/cos(psi)` 投影，又用 `rot_y(psi)` 旋转，导致力完全错误。结果不稳定（推力 -395 mN，俯仰 76°）。**不要参考此版本核心逻辑。** |
| v5-fixed | `pitch_dynamics_v5_fixed.py` | **当前主力** | 固定点 + 俯仰自由度 + 四分量模型 + 56 组攻角扫描 | 工作正常，完成基线扫描 |
| v5-moving | `pitch_dynamics_v5_moving.py` | **框架已写，未运行** | 在 v5-fixed 基础上引入水平速度 Vx，状态 `[θ_p, θ̇_p, x, Vx]` | 待运行、待验证 |

### 2.2 关键教训（必须牢记）

1. **急回 + 对称攻角 = 灾难**：由于 `cw` 旋转时下拍快、上拍慢，若上下拍使用对称攻角（如 ±45°），上拍的负升力（绝对值更大且持续时间更长）会压倒下拍的正升力，导致**净负升力**。必须使用**非对称攻角**（下拍正攻角大，上拍负攻角小或中等）来补偿。

2. **v4 的双重投影陷阱**：v4 中同时使用了
   - 投影公式：`Fx = sin(ψ)(sign·D - L)`
   - 旋转矩阵：`R_y(ψ) @ [Fx_wing, 0, Fz_wing]`
   这相当于对同一个力旋转了两次，完全错误。**v5 已修正为仅使用投影公式（v3 验证过）。**

3. **RK4 标量化问题**：`compute_rhs()` 在接收 1 元素 slice（如 `phi[i:i+1]`）时返回 array 而非 scalar，导致 RK4 状态更新出错。v5 已加入显式标量化：
   ```python
   if np.ndim(theta_ddot) > 0:
       theta_ddot = float(theta_ddot[0])
   ```

---

## 3. 当前核心物理模型

### 3.1 坐标系定义

- **惯性系 I**：原点在地面，X_I 水平向前（蝴蝶头方向），Z_I 垂直向上。
- **机体坐标系 B**：原点在俯仰轴（重心附近），x_B 沿机身向前，z_B 垂直向上，y_B 沿翼展向右（右手系：x × y = z）。
- **翅膀坐标系 W**：绕 y_B 旋转 `ψ = φ + θ_p`，φ 为机构拍动角，θ_p 为机身俯仰角。

> 在 v5-fixed 中，机体固定在空间中，因此 B 系与 I 系对齐（无平移）。v5-moving 中 B 系原点会随 x 移动，但气动力仍在 B 系中计算。

### 3.2 四分量气动力模型（Sane & Dickinson 2002）

```
F_total = F_trans + F_AM + F_rot + F_clap
```

#### 1) 平动升力/阻力（Translational）
- 相对速度：`U = |Ω| · R`，其中 `Ω = φ̇ + θ̇_p`
- 动压项：`const = 0.5 · ρ · U² · S · r₂² · k_3d`
- `L_trans = const · C_L(α_eff)`
- `D_trans = const · C_D(α_eff)`

#### 2) 附加质量力（Added Mass）
```
F_AM = -(ρ · π · c_avg² / 4) · φ̈ · R · r₁ · sin(α)
```
方向垂直于翅膀平面（与升力同向基准）。这里使用了 `mechanism.py` 输出的 `φ̈`（`phi_ddot`）。

#### 3) 旋转力（Rotational / Kramer 效应）
```
F_rot = ρ · C_rot · α̇ · φ̇ · c_avg² · R · r_rot
```
**当前机构无扭转自由度，α̇ = 0，因此 F_rot ≡ 0。** 若未来引入被动扭转或主动扭转，此力将自然激活。

#### 4) Clap-and-Fling
当 `|φ̇| < 0.1 · |φ̇|_peak`（反转点附近），升力类分量增强 `k_clap = 1.3` 倍。

### 3.3 攻角选择规则（核心！）

```python
mask_down = phi_dot <= 0   # 下拍
if 下拍: alpha_eff = +alpha_down   # 如 +40° ~ +60°
if 上拍: alpha_eff = -alpha_up     # 如 -5° ~ -60°（取负值）
```

**注意**：攻角基于 `phi_dot`（机构运动方向）选择，而非 `Ω = phi_dot + theta_dot`。这是因为在物理上，翅膀的“上下拍”由机构运动决定，机身俯仰只是叠加了一个刚体转动。

### 3.4 力投影公式（v3 验证，v5 使用）

```python
# psi = phi + theta_p
# sign_Omega = +1 (上拍, Omega > 0) 或 -1 (下拍, Omega < 0)
Fx = sin(psi) * (sign_Omega * D_eff - L_eff)
Fz = cos(psi) * (L_eff - sign_Omega * D_eff)
```

物理验证：
- `ψ = 0`（水平）：`Fx = 0`，`Fz = L - sign·D`（纯垂直力）
- `ψ = 90°`（垂直）：`Fx = sign·D - L`，`Fz = 0`（纯水平力）
- **下拍**（`sign = -1`）：`Fz = L + D > 0`（向上升力 ✅）
- **上拍**（`sign = +1`）：若 `L < D`，则 `Fx = D - L > 0`（向前推力 ✅）

### 3.5 俯仰动力学方程

```
M_aero = 2 * (-x_front * Fz_front - x_back * Fz_back)
M_gravity = -m_total * g * d_cg * sin(theta_p)
M_damp = -c_damp * theta_dot
theta_ddot = (M_aero + M_gravity + M_damp) / I_yy
```

其中 `x_front = +0.025 m`，`x_back = -0.025 m`，`d_cg = 0.015 m`（重心到俯仰轴距离），`I_yy = 3e-5 kg·m²`。

### 3.6 升阻力系数经验公式（必须限制范围）

```python
C_L = 0.255 + 1.58 * sin(2.13° * alpha - 7.2°)
C_D = 1.92 - 1.55 * cos(2.04° * alpha - 9.82°)
```

**有效范围**：`|alpha| ≤ 60°`。代码中已用 `clamp_alpha()` 强制截断。**绝对不要让 alpha 超出此范围使用经验公式。**

---

## 4. 翅膀几何参数（来自 DXF）

由 `src/analyze_dxf.py` 读取 `data/WingFront.DXF`、`data/WingBack.DXF`、`data/WingsAxis.DXF` 计算得到。

| 参数 | Front | Back | 单位 |
|------|-------|------|------|
| 面积 S | 0.01617 | 0.01554 | m² |
| 展长 R | 0.1543 | 0.1474 | m |
| 平均弦长 c_avg | 0.1048 | 0.1054 | m |
| 一阶面积矩 r₁ | 0.4227 | 0.4798 | — |
| 二阶面积矩 r₂² | 0.2382 | 0.2876 | — |
| 展弦比 AR | 1.472 | 1.399 | — |

弦长分布保存在 `output/tables/chord_distribution.csv`，200 个展向分段，包含 `y_hat`（归一化展向 0~1）、`c_hat`（归一化弦长）、实际弦长（mm）。**当前 v5 代码尚未使用 blade-element 方法，而是使用了总气动力模型（基于平均弦长和总面积）。若未来需要更高精度，应按 `c(y)` 沿展向积分。**

---

## 5. 当前成果与关键数据

### 5.1 v5-fixed 攻角扫描结果（56 组组合）

扫描范围：`alpha_down ∈ [20, 30, 40, 45, 50, 55, 60]°`，`alpha_up ∈ [-5, -10, -15, -20, -30, -40, -50, -60]°`

**按升重比 L/W 排序的前 10 名（基线同相，t_end=1.5s）：**

| ad | au | Lift (mN) | Thrust (mN) | L/W | \|Pitch\| (°) |
|----|----|-----------|-------------|-----|--------------|
| 60 | -5 | +123.7 | -99.3 | **0.635** | 2.5 |
| 55 | -5 | +114.6 | -83.9 | 0.588 | 2.4 |
| 50 | -5 | +102.2 | -66.3 | 0.524 | 2.2 |
| 60 | -10 | +100.4 | -42.2 | 0.515 | 2.7 |
| 45 | -5 | +87.3 | -47.6 | 0.448 | 2.1 |
| 55 | -10 | +92.6 | -29.4 | 0.475 | 2.5 |
| 60 | -15 | +78.8 | +3.8 | 0.404 | 3.0 |
| 50 | -10 | +82.7 | -15.2 | 0.424 | 2.3 |
| 60 | -20 | +60.1 | +40.2 | 0.308 | 3.4 |
| 45 | -10 | +69.4 | +0.5 | 0.356 | 2.2 |

> 注：重量 `W = m·g = 0.020 · 9.81 = 0.1962 N = 196.2 mN`。L/W = 0.635 表示平均升力约 123.7 mN，**尚未达到悬停所需的 1.0**。

### 5.2 关键问题：最佳基线参数在反相时不稳定

使用 **ad=60°, au=-5°**（最佳基线）测试反相（anti-phase 180°）时：
- 平均升力：约 **-507 mN**（严重向下！）
- 最大俯仰角：**73.7°**
- 状态：完全不稳定，俯仰剧烈发散。

**原因分析**：au=-5° 时上拍产生极大负升力（L_eff 为负且绝对值大），在反相配置下前后翅负升力叠加，无恢复机制。

### 5.3 更"平衡"的参数组合（可能更适合反相）

从扫描数据观察，一些**升力和推力均为正**的组合：

| ad | au | Lift (mN) | Thrust (mN) | L/W | 特点 |
|----|----|-----------|-------------|-----|------|
| 60 | -40 | +77.7 | +24.0 | 0.396 | 升/推均正，可能反相稳定 |
| 55 | -40 | +72.3 | +61.7 | 0.368 | 推力更强 |
| 50 | -40 | +62.5 | +90.0 | 0.319 | 推力主导 |
| 60 | -30 | +90.8 | -13.0 | 0.463 | 升力较好，推力略负 |

**下一步任务**：用这些"平衡型"参数测试反相稳定性，找到**既能基线悬停、又能在反相下稳定**的参数组合。

---

## 6. 文件结构与必读文件

```
D:\code\Butterfly
├── src/
│   ├── mechanism.py              # 【必读】曲柄摇杆机构运动学，输出 phi, phi_dot, phi_ddot
│   └── analyze_dxf.py            # 【必读】读取 DXF，计算翅膀几何参数、弦长分布
├── data/
│   ├── WingFront.DXF             # 前翅 DXF 图纸
│   ├── WingBack.DXF              # 后翅 DXF 图纸
│   ├── WingsAxis.DXF             # 翅膀转轴 DXF 图纸
│   └── wing_analysis_results.json # analyze_dxf 输出：面积、展长、面积矩等
├── output/tables/
│   └── chord_distribution.csv    # 200 段展向弦长分布（blade-element 基础）
├── temp/                          # 【已加入 Git 版本管理】
│   ├── pitch_dynamics_v5_fixed.py # 【当前主力】固定点俯仰动力学
│   ├── pitch_dynamics_v5_moving.py # 【待运行】移动 + 俯仰动力学框架
│   ├── debug_v4_forces.py        # 力分量诊断脚本
│   ├── case*_v5_fixed.png        # v5 基线/反相/90°/变频 的仿真结果图
│   └── scan_*.png                # 56 组攻角扫描结果图
└── HANDOVER_PITCH_DYNAMICS.md    # 本文档
```

### 6.1 阅读优先级
1. **`temp/pitch_dynamics_v5_fixed.py`** — 理解当前完整的气动力计算、RK4 积分、攻角扫描逻辑。
2. **`src/mechanism.py`** — 理解 phi/phi_dot/phi_ddot 的来源、急回特性、角度偏移。
3. **`src/analyze_dxf.py`** — 理解翅膀几何提取方式、r₁/r₂² 的计算方法。
4. **`data/wing_analysis_results.json`** — 核对几何参数数值。
5. （可选）`temp/pitch_dynamics_v4.py` — 仅用于理解 v4 犯了什么错误，不要复制其逻辑。

---

## 7. 待办事项（TODO）与优先级

### 🔴 高优先级

1. **寻找反相稳定参数**
   - 使用扫描中升/推均正的参数（如 ad=60/au=-40, ad=55/au=-40 等）运行 v5-fixed 的反相 case。
   - 若反相仍不稳定，缩小扫描步长（如 ad ∈ [50,55,60], au ∈ [-30,-35,-40,-45]）精细搜索。
   - **目标**：找到至少一组参数，使得基线 L/W > 0.4 且反相最大俯仰角 < 30°。

2. **运行 v5-moving**
   - 当前 `pitch_dynamics_v5_moving.py` 框架已写，但**从未运行**。
   - 状态向量：`[theta_p, theta_dot, x, Vx]`
   - 需要验证：从 Vx=0 开始，气动力 Fx 能否推动蝴蝶进入前飞状态？
   - 注意：当前模型简化为"小前飞速度下忽略前飞对相对速度方向的修正"。若 Vx 增长到与拍动速度相当（如 > 0.5 m/s），此简化失效，需升级。

3. **升重比不足问题**
   - 当前最佳 L/W ≈ 0.635，距离悬停（1.0）还差约 37%。
   - 可能改进方向：
     - 提高频率（当前 15 Hz，试 20~25 Hz）
     - 优化机构参数 `a`（改变摆幅和急回比）
     - 引入 blade-element 方法（利用 chord_distribution.csv 沿展向积分，考虑局部速度差异）
     - 重新校核 `k_3d`（三维效应修正系数，当前 0.7）
     - 检查 `I_yy` 和 `d_cg` 的物理合理性

### 🟡 中优先级

4. **Blade-Element 升级**
   - 当前使用的是总气动力模型（基于平均弦长 `c_avg` 和总面积 `S`）。
   - 更精确的做法：沿展向 200 段，每段计算局部速度 `U(y) = |Ω| · y · R`，局部弦长 `c(y)`，局部力 `dL = 0.5 ρ U² c(y) dy · C_L`，然后积分。
   - 参考数据：`output/tables/chord_distribution.csv`

5. **前飞效应修正**
   - v5-moving 中当前忽略前飞速度对相对速度方向的修正。
   - 严格处理：相对速度 `v_rel = v_flap + v_body`，其中 `v_body = [Vx, 0, 0]`（机体前飞速度）。
   - 需要重新推导升阻方向（不再简单垂直/平行于翅膀平面，而是相对于来流方向）。

6. **旋转力 F_rot 的激活**
   - 当前 `alpha_dot = 0`。若机构引入被动扭转（elastic twist）或主动扭转，需计算 `alpha_dot` 并激活 `F_rot`。

### 🟢 低优先级 / 未来方向

7. **优化安装角**
   - 当前假设固定安装角（通过 `alpha_down`/`alpha_up` 体现）。可引入优化器（如网格搜索、贝叶斯优化）自动搜索最佳攻角组合。

8. **多自由度扩展**
   - 当前只有俯仰 + 水平移动。未来可能需要：垂直升降（z）、滚转（roll）、偏航（yaw）。

9. **与实验对比**
   - 若用户有风洞实验或自由飞行数据，用实验数据校准 `k_3d`、`C_rot`、`c_damp` 等经验参数。

---

## 8. 运行方法

### 8.1 环境
- Python 3.x
- 依赖：`numpy`, `matplotlib`, `scipy`, `pandas`

### 8.2 运行 v5-fixed
```bash
cd D:\code\Butterfly
python temp/pitch_dynamics_v5_fixed.py
```
- 默认会先执行 `alpha_scan()`（56 组组合，约需几分钟）。
- 然后使用最佳参数运行 4 个 case（基线、反相 180°、反相变频、90° 相位差）。
- 结果图保存在 `temp/case*_v5_fixed.png`，扫描图保存在 `temp/scan_*.png`。

### 8.3 运行 v5-moving
```bash
cd D:\code\Butterfly
python temp/pitch_dynamics_v5_moving.py
```
- 当前框架使用硬编码参数 `alpha_down=45°, alpha_up=-10°`。
- 建议先找到稳定的反相参数后，再修改此文件中的 `base` 字典并运行。

### 8.4 重新提取翅膀几何
```bash
cd D:\code\Butterfly
python src/analyze_dxf.py
```
- 输出：`data/wing_analysis_results.json`、`output/tables/chord_distribution.csv`、`output/figures/wing_analysis.png`

---

## 9. 常见问题与调试技巧

### Q1: 为什么升力是负的？
- 检查攻角选择是否正确（下拍用正，上拍用负）。
- 检查 `phi_offset_deg` 是否正确（应为 -50.84°，使拍动关于水平对称）。
- 检查是否使用了对称攻角（如 ±45°），急回特性会导致净负升力。

### Q2: 为什么反相不稳定？
- 反相时前后翅运动相反，若上拍产生强负升力（如 au=-5°），一侧上拍时另一侧下拍，负升力侧可能压倒正升力侧，且无恢复力矩。
- 尝试使用升/推均正的"平衡型"参数（au 更负，如 -30° ~ -40°）。

### Q3: RK4 积分发散？
- 检查 `dt` 是否足够小。当前 `dt = 2.0s / 40000 = 50 μs`，通常足够。
- 检查 `compute_rhs()` 返回的是否为标量（v5 已修复）。

### Q4: 力矩方向不确定？
- 前翅力臂 `x_front = +0.025 m`（在俯仰轴前方），后翅 `x_back = -0.025 m`（在后方）。
- `M_y = -x_front * Fz_front - x_back * Fz_back`。
- 若前翅产生向上升力（Fz > 0），力矩为负（低头）。若后翅产生向上升力，力矩为正（抬头）。

---

## 10. 参数速查表

```python
# 物理参数（v5-fixed & v5-moving 共用）
PHYS = {
    "rho": 1.225,       # 空气密度 kg/m³
    "g": 9.81,          # 重力加速度 m/s²
    "m_total": 0.020,   # 总质量 kg (20 g)
    "I_yy": 3e-5,       # 俯仰转动惯量 kg·m²
    "x_front": 0.025,   # 前翅力臂 m
    "x_back": -0.025,   # 后翅力臂 m
    "d_cg": 0.015,      # 重心到俯仰轴距离 m
    "c_damp": 5e-4,     # 俯仰阻尼 N·m·s/rad
}

# 气动参数
AERO = {
    "k_3d": 0.7,        # 三维效应修正系数
    "C_rot": 1.5,       # 旋转力系数
    "r_rot": 0.5,       # 旋转力作用点
    "k_clap": 1.3,      # Clap-and-Fling 增强倍数
    "alpha_max_deg": 60.0,  # 经验公式上限
}

# 机构参数
mechanism.DEFAULT_PARAMS = {
    "a": 7.92,          # 翅膀转轴 y 坐标 mm
    "b": 6.97,          # 曲柄圆心 x 坐标 mm
    "R": 2.25,          # 曲柄半径 mm
    "c": 14.00,         # 连杆长度 mm
    "l": 8.00,          # 摇杆/翅膀杆长度 mm
    "phi_offset_deg": -50.84,  # 安装基准偏移
}

# 重量
W = 196.2 mN  # (0.020 * 9.81 * 1000)
```

---

## 11. Git 提交记录（最近）

```
330eec0 v5: fixed point model with alpha scan and moving framework
2954c9a temp: pitch dynamics v3/v4 and diagnostics
e19fb8a fix/rewind: crank-rocker mechanism + aerodynamic model fixes (#4)
790b44e Feature/gear analysis (#3)
84f24e9 feat: mechanism angle inversion, power analysis, and raw vs scaled kinematics (#2)
```

**当前 `.gitignore` 已移除 `temp/` 的忽略规则**，所有 temp 代码和结果图已纳入版本管理，方便回溯。

---

## 12. 联系用户前的自检清单

在询问用户之前，请确认：
- [ ] 已完整阅读 `temp/pitch_dynamics_v5_fixed.py` 的 `compute_forces()` 和 `compute_rhs()`。
- [ ] 已理解攻角选择基于 `phi_dot` 而非 `Omega`。
- [ ] 已理解力投影公式只使用了一次（无双重投影）。
- [ ] 已检查 `alpha` 是否被 `clamp_alpha()` 限制在 ±60°。
- [ ] 已确认 `WING_BACK` 的面积单位是 `m²`（`e-6`），不是 `e-3`。
- [ ] 已阅读 `src/mechanism.py` 理解急回特性。
- [ ] 已查看 `data/wing_analysis_results.json` 中的几何参数。

---

*本文档尽可能完整地记录了当前状态。如有遗漏，请查阅 `temp/` 目录下的历史版本代码和输出图片，以及 Git 提交历史。*
