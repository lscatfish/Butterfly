# Butterfly Aerodynamic Analysis — Agent Notes

## Project Overview
仿生蝴蝶（机械原理课程作业）翅膀气动参数提取与准定常力分解。
- 总质量 20 g，四翅总质量 4 g
- 扑动频率 15–20 Hz（典型值 15.0 Hz），机构原始角度不做缩放
- **运动学由前置连杆机构（mechanism.py）生成**
- 需分别计算前翅、后翅的升力/阻力/旋转力/附加质量力

## File Structure
```
├── cad/                          SolidWorks 文件
│   └── Wings.SLDPRT              零件（含草图 100/101/102）
├── data/                         原始数据 + 分析结果
│   ├── WingFront.DXF             前翅草图（SPLINE×3 + LINE×1）
│   ├── WingBack.DXF              后翅草图（SPLINE×1 + LINE×1）
│   ├── WingsAxis.DXF             转轴草图（CIRCLE×2，定义 hinge line）
│   └── wing_analysis_results.json 几何参数与静态估算结果
├── src/                          Python 分析脚本
│   ├── analyze_dxf.py            主分析脚本（几何提取 + 静态气动力）
│   ├── mechanism.py              前置机构运动学模块（crank-rocker → φ(t)）
│   ├── dynamic_analysis.py       动态气动力分析（机构运动学 + 参数扫描）
│   ├── alpha_scan.py             安装角扫描（净升力/阻力/升阻比 vs α）
│   ├── mechanism_plot.py         机构可视化（轨迹 + a扫描 @17.5Hz）
│   ├── plot_wing_shape.py        翅膀与转轴平面形状绘制（DXF → 可视化）
├── output/                       生成的输出文件
│   ├── figures/                  图表（PNG）
│   │   ├── wing_analysis.png     几何与弦长分布（analyze_dxf.py）
│   │   ├── wing_shape.png        翅膀与转轴平面形状（plot_wing_shape.py）
│   │   ├── force_vs_phi.png      力随转角变化（dynamic_analysis.py）
│   │   ├── force_time_domain.png 时间域力曲线（dynamic_analysis.py）
│   │   ├── wing_acceleration.png 角速度/角加速度（dynamic_analysis.py）
│   │   ├── param_scan.png        参数扫描（dynamic_analysis.py）
│   │   ├── alpha_scan.png        安装角扫描（alpha_scan.py）
│   │   ├── mechanism_kinematics.png 机构自检图（mechanism.py）
│   │   └── mechanism_analysis.png 机构可视化（mechanism_plot.py @17.5Hz）
│   ├── tables/                   数据表（CSV）
│   │   └── chord_distribution.csv 弦长分布
│   └── reports/                  报告（Markdown）
│       └── 气动分析报告.md
├── docs/                         文档
│   ├── AGENTS.md                 本文件（根目录 symlink/copy）
│   ├── mechanism.md              mechanism.py 说明（四连杆运动学）
│   ├── mechanism_plot.md         mechanism_plot.py 说明（可视化）
│   ├── dynamic_analysis.md       dynamic_analysis.py 说明（气动力分析）
│   ├── gear_analysis.md          gear_analysis.py 说明（减速轮系）
│   ├── analyze_dxf.md            analyze_dxf.py 说明（DXF 几何提取）
│   ├── alpha_scan.md             alpha_scan.py 说明（攻角扫描）
│   ├── plot_wing_shape.md        plot_wing_shape.py 说明（翅膀形状绘制）
│   └── 仿生蝴蝶翅膀空气动力学分析文献综述.md
└── .gitignore
```

## Critical Pitfall: DXF Export Only
- **绝对不要用 VBA `GetSketchSegments`**：返回的是全局坐标，且包含 20 m 长的参考构造线，导致数据完全错乱。
- **正确做法**：在 SolidWorks 中激活目标草图 → 文件 → 另存为 → DXF (R2000+) → 选项 → 仅输出激活草图 → 保存。
- 这样得到的 DXF 使用**草图局部坐标**（mm 量级，而非全局坐标 10,000 mm 量级）。

## DXF Parsing
- 使用自定义轻量级 parser（不依赖 ezdxf），解析 SPLINE / LINE / CIRCLE。
- SPLINE 用 scipy.interpolate.BSpline 采样为密集点（默认 2000 点）。
- 多个 SPLINE/LINE 段通过 nearest-neighbor 首尾相连（tolerance 0.1 mm）。
- **注意**：2026-05-29 修复了 parser 在遇到 code 0 时未终止当前实体块的 bug，避免 knots 数组被后续实体数据污染。

## Coordinate System
- 转轴由 `WingsAxis.DXF` 中两个圆的圆心定义：`p0` 和 `p1`。
- `unit_dir = (p1-p0)/|p1-p0|`：沿转轴方向（展向近似）。
- `unit_perp = (-unit_dir[1], unit_dir[0])`：垂直于转轴方向（弦向近似）。
- 局部坐标：
  - `r = (pts - p0) @ unit_dir`：沿转轴投影（弦向坐标）
  - `y = (pts - p0) @ unit_perp`：垂直转轴投影（展向坐标）
- 展长 `R = y_max - y_min`，面积通过沿展向分条积分弦长得到。

## Geometry Parameters (最新结果，2026-05-29)
| 参数 | 前翅 Front | 后翅 Back |
|------|-----------|-----------|
| 面积 S (mm²) | 16,166.6 | 15,537.6 |
| 展长 R (mm) | 154.3 | 147.4 |
| 平均弦长 c_avg (mm) | 104.8 | 105.4 |
| 展弦比 AR | 1.47 | 1.40 |
| 二阶面积矩 r̂₂² | 0.2382 | 0.2876 |
| 一阶面积矩 r̂₁ | 0.4227 | 0.4798 |

> **注意**：轴线 DXF 在 2026-05-29 导出后，第二个圆心变为 (41.55, 44.73)，导致轴线长度 140.84 mm。前一次导出时第二个圆心为 (0, 0)，长度 86 mm。几何结果随之变化。当前使用最新 DXF 数据。

## Aerodynamic Model
基于文献综述的准定常分解（Dickinson 2001 / Sane & Dickinson 2002）。

### 1. 运动学参数
- **由前置连杆机构（mechanism.py）生成**：曲柄摇杆四连杆机构，曲柄一周=翅膀一拍
- 典型频率 `f = 15.0 Hz`（范围 15–20 Hz），机构原始摆幅 a=7.92 时为 **28.6° ~ 73.1°**（span=44.5°）
- 翅膀安装基准偏移 `φ_offset = -50.84°`（出厂固定折弯角），使运动关于水平对称：偏移后范围 **-22.2° ~ +22.2°**
- 可调参数：`a`（飞行中可调，控制摆幅）和频率 `f`
- 固定参数：`b=6.97`, `R=2.25`, `c=14`, `l=8`
- 当前 (a=7.92, f=15.0Hz, cw): `φ̇_max = 44.2 rad/s`, `φ̈_max = 5,730 rad/s²`

### a 扫描表 (f=15.0 Hz, cw, 含默认偏移 -50.84°)
| a | span | 偏移后范围 | φ̇_max | φ̈_max |
|---|------|-----------|--------|--------|
| 6.0 | 55.8° | [-5.7°, 50.1°] | 61.2 | — |
| 7.0 | 49.0° | [-14.5°, 34.5°] | 50.7 | — |
| **7.92** | **44.5°** | **[-22.2°, 22.2°]** | **44.2** | **5,730** |
| 9.0 | 40.5° | [-30.8°, 9.7°] | 38.8 | — |
| 10.0 | 37.8° | [-38.4°, -0.7°] | 35.1 | — |
| 11.0 | 35.7° | [-45.8°, -10.1°] | 32.3 | — |

> 仅 **a=7.92** 配合固定偏移 -50.84° 时关于 0° 对称。偏移为出厂固定值，不随 a 改变。

### 2. 气动力系数
- 升力系数：`C_L(α) = 0.255 + 1.58·sin(2.13·α - 7.2°)`，峰值 ~1.83 @ α≈45°
- 阻力系数：`C_D(α) = 1.92 - 1.55·cos(2.04·α - 9.82°)`，峰值 ~3.47 @ α≈90°
- 旋转力系数：`C_rot = 1.5`（公式保留，当前固定攻角 α̇=0 时自然为 0）

### 3. 力分解公式
| 分量 | 公式 | 状态 |
|------|------|------|
| 平动升力 | `0.5·ρ·C_L·(φ̇·R)²·S·r̂₂²` | ✅ |
| 平动阻力 | `0.5·ρ·C_D·(φ̇·R)²·S·r̂₂²` | ✅ |
| 旋转力 | `ρ·C_rot·α̇·φ̇·c_avg²·R·r_rot` | ✅（α̇=0 时自然为 0）|
| 附加质量力 | `-(ρ·π·c_avg²/4)·φ̈·R·r̂₁·sin(α)` | ✅ |
| Clap-and-Fling | reversal 附近升力 × `k_clap`（默认 1.3） | ✅ |
| 三维展向修正 | 整体气动力 × `k_3d`（默认 0.7） | ✅ |

> **重要假设**：
> 1. 运动学由前置连杆机构（mechanism.py）生成，曲柄一周=翅膀一拍。
> 2. **固定攻角假设**：机械蝴蝶的翅膀通过刚性结构安装，安装角度固定为 `α = +45°`，扑动过程中**无法主动翻转攻角**。单次周期分析中攻角不变，但可外部扫描不同 α。
> 3. **上拍升力方向反转**：由于攻角固定，平动升力方向由 φ̇ 符号决定。φ̇ ≤ 0（向下）→ 攻角 +α → C_L > 0；φ̇ > 0（向上）→ 攻角 -α → C_L < 0。
> 4. 附加质量力：`F_AM = -(ρπc²/4)·φ̈·R·r̂₁·sin(α)`，峰值出现在 stroke reversal，时均≈0（对称抵消）。
> 5. 三维展向效应通过全局修正系数 `k_3d=0.7` 量化（附加质量力不受影响）。

### 4. 力计算结果（机构运动学，a=7.92, b=6.97, R=2.25, c=14, l=8, φ_offset=-50.84°）

**动态分析**（`dynamic_analysis.py`，含 Clap-and-Fling + 三维修正 k_3d=0.7）：
| 项目 | 四翅总计 |
|------|---------|
| 重量 | 245.3 mN |
| 时均升力（绝对值）| 4,695 mN |
| 净升力（符号平均）| **~10 mN** |
| 时均阻力 | 223 mN |
| 升重比（绝对值）| 19.1 |
| 净升重比 | **0.04** |

> **核心问题**：固定攻角导致下拍正升力与上拍负升力几乎完全抵消，净升力仅约 10 mN，远不足以支撑 245 mN 重量。
> - 时均升力（绝对值）高达 4,695 mN，说明拍动产生的瞬时力很大；
> - 但由于对称的上下拍运动 + 固定攻角，符号平均后几乎为零；
> - 实际蝴蝶通过主动扭转翅膀（pitch reversal）使上下拍均产生正升力，机械结构暂无法实现。

### 5. 功率需求（2026-06-03）

**计算公式**：
- 转动惯量：`I_w = m_w · R² · r̂₂²`（单翅 `m_w = 1 g`）
- 气动功率：`P_aero = F_drag × |φ̇| × R × r̂₁` — 克服空气阻力
- 惯性功率：`P_inertial = I_w × φ̈ × φ̇` — 加速/减速翅膀（周期平均≈0）

**动态分析功率结果**（四翅总计，f=15.0 Hz，含 k_3d=0.7）：
| 项目 | 数值 | 说明 |
|------|------|------|
| 时均气动功率 | **506 mW** | 持续悬停的主要能耗 |
| 峰值气动功率 | **~2,100 mW** | 拍动中期 |
| 峰值惯性功率 | **~3,400 mW** | stroke reversal |
| **峰值总功率** | **~1,850 mW** | **电机需提供的最大功率** |

> **功率评估**：
> - 时均气动功率 506 mW 是持续悬停的主要能耗
> - 峰值总功率 ~1.85 W 决定电机和减速器选型（需留裕量）
> - 惯性功率在 stroke reversal 达到峰值，与角加速度同步，但周期平均≈0
> - 对于 20g 微型飞行器，1.85 W 峰值功率意味着电机功率密度需达 ~0.093 W/g
> - 旧模型中 32.9 W 为错误结果（单位/公式 bug 已修复）

## Design Parameters Table
| 参数 | 值 | 来源/假设 |
|------|-----|----------|
| 总质量 m_total | 20 g | 用户给定 |
| 四翅总质量 m_wing_total | 4 g | 用户给定（单翅 1 g） |
| 扑动频率 f | 15.0 Hz | 用户范围 15–20 Hz |
| 机构原始角度范围 (a=7.92) | **28.6° ~ 73.1°** | 机构自然输出，span=44.5° |
| 翅膀安装偏移 φ_offset | **-50.84°** | 出厂固定折弯角，使运动关于水平对称 |
| 偏移后角度范围 | **-22.2° ~ +22.2°** | 实际气动计算使用此范围 |
| 攻角 α | 45° | 固定安装角（单次周期内不变，可外部扫描） |
| 旋转力系数 C_rot | 1.5 | 公式保留，当前 α̇=0 时自然为 0 |
| Clap-and-Fling 系数 k_clap | 1.3 | 简化模型，reversal 附近升力增强 |
| 三维修正系数 k_3d | 0.7 | 整体气动力降低 30% |
| 空气密度 ρ | 1.225 kg/m³ | 标准海平面 |
| 运动粘度 ν | 1.46e-5 m²/s | 标准海平面 |
| 机构参数 a | 7.92（飞行中可调） | A 点 y 坐标，控制摆幅 |
| 机构参数 b | 6.97（固定） | B 点 x 坐标（曲柄圆心） |
| 机构参数 R | 2.25（固定） | 曲柄半径 |
| 机构参数 c | 14.00（固定） | 连杆长度 |
| 机构参数 l | 8.00（固定） | 摇杆/翅膀杆长度 |

## Dynamic Analysis (`dynamic_analysis.py`)

1. **机构运动学**：由 `mechanism.py` 生成 φ(t)。可调参数 a，b/R/c/l 固定。
2. **固定攻角模型**：刚性安装，攻角 +45° 单次周期内不变，α̇=0。
3. **C_L 基于瞬时速度方向**：φ̇ ≤ 0 → C_L(+α)；φ̇ > 0 → C_L(-α)。
4. **F_AM = -(ρπc²/4)·φ̈·R·r̂₁·sin(α)**。
5. **旋转力**：公式保留 `F_rot = ρ·C_rot·α̇·φ̇·c²·R·r_rot`，当前 α̇=0 时自然为 0。
6. **Clap-and-Fling**：简化模型，stroke reversal 附近（|φ̇| < 10% 峰值）升力增强 `k_clap` 倍。
7. **三维展向修正**：整体气动力乘以 `k_3d=0.7`（附加质量力不受影响）。
8. **功率计算**：
   - 气动功率 `P_aero = F_drag × |φ̇| × R × r̂₁`
   - 惯性功率 `P_inertial = I_w × φ̈ × φ̇`
   - 峰值总功率 ~1.85 W（四翅）
9. **参数扫描**：频率 10-25 Hz、机构参数 a 6-12、攻角 20-70°。
10. **图表**：`force_vs_phi.png`、`force_time_domain.png`、`wing_acceleration.png`、`power_time_domain.png`、`param_scan.png`。
11. **Markdown 报告**：`气动分析报告.md`。

## `mechanism_plot.py`
机构运动学可视化（已同步至新版四连杆模型）：
- 左上：机构几何示意图（固定点、轨迹圆、若干姿态）
- 中上：cw vs ccw 角度 φ(t) 对比
- 右上：cw vs ccw 角速度 φ̇(t) 对比
- 左下：cw vs ccw 角加速度 φ̈(t) 对比
- 中下：a 扫描 φ(t) 重叠 @ 实际频率
- 右下：a 扫描 φ̇(t) 重叠 @ 实际频率

默认自动应用 `DEFAULT_PARAMS['phi_offset_deg'] = -50.84°`。

用法：`python src/mechanism_plot.py --freq 15 --a 7.92 --a-list 6 7 8 9 10 11`

## `alpha_scan.py`
安装角 α 扫描（5-85°），计算净升力（符号平均）和升阻比：

| α | 净升力(mN) | 阻力(mN) | L/D | vs 重量 | 说明 |
|---|-----------|----------|-----|---------|------|
| 17° | -0.3 | 75.9 | -0.004 | 0.0x | — |
| 35° | 5.5 | 162.7 | 0.034 | 0.0x | — |
| **45°** | **13.9** | 222.3 | 0.063 | **0.1x** | **当前设计** |
| 55° | 25.0 | 280.8 | 0.089 | 0.1x | |
| 75° | 48.9 | 366.0 | 0.134 | 0.2x | 最大净升力 |
| 85° | **58.6** | 382.1 | **0.153** | 0.2x | 最佳效率（但升力仍不足） |

> **注意**：固定攻角下净升重比极低（~0.04），与 α 扫描结果趋势一致。增大 α 可提高净升力绝对值，但升阻比下降。核心瓶颈是固定攻角导致的下拍/上拍升力抵消，非机构摆幅问题。

### 转角约定
- 机构运动学：曲柄一周 = 翅膀一拍，**原始输出不做缩放**
- `φ < 0`：下拍（翅膀向下运动）
- `φ > 0`：上拍（翅膀向上运动）
- 固定攻角 α = +45°，无翻转

> **重要**：早期 VBA 导出的 `wing_axis.csv`、`wing_back.csv`、`wing_front.csv` 已废弃（全局坐标，含 20m 构造线），已重命名为 `_old`。所有分析现在严格基于 DXF 文件。

## Git
- `.gitignore` 已配置，忽略 `*.png`, `*.json`, `*.csv`, Python cache, SolidWorks 临时文件。
- 但 `气动分析报告.md` 和 `wing_analysis_results.json` 建议保留（分析结果）。
