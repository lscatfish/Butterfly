# Butterfly Aerodynamic Analysis — Agent Notes

## Project Overview
仿生蝴蝶（机械原理课程作业）翅膀气动参数提取与准定常力分解。
- 总质量 25 g，四翅总质量 4 g
- 扑动频率 15–20 Hz（典型值 17.5 Hz），下拍 80° 上拍 60°
- **运动学由前置连杆机构（mechanism.py）生成**
- 需分别计算前翅、后翅的升力/阻力/旋转力/附加质量力

## File Structure
```
├── cad/                          SolidWorks 文件
│   └── Wings.SLDPRT              零件（含草图 100/101/102）
├── data/                         原始数据 + 分析结果
│   ├── WingFront.DXF             前翅草图（SPLINE×3 + LINE×1）
│   ├── WingBack.DXF              后翅草图（SPLINE×1 + LINE×1）
│   ├── WingsAxis.DXF             转轴草图（CIRCLE×2，定义 hinge line）
│   └── wing_analysis_results.json 几何参数与静态估算结果
├── src/                          Python 分析脚本
│   ├── analyze_dxf.py            主分析脚本（几何提取 + 静态气动力）
│   ├── mechanism.py              前置机构运动学模块（crank-rocker → φ(t)）
│   ├── dynamic_analysis.py       动态气动力分析（机构运动学 + 参数扫描）
│   ├── alpha_scan.py             安装角扫描（净升力/阻力/升阻比 vs α）
│   ├── mechanism_plot.py         机构可视化（轨迹 + a扫描 @17.5Hz）
│   ├── plot_wing_shape.py        翅膀与转轴平面形状绘制（DXF → 可视化）
├── output/                       生成的输出文件
│   ├── figures/                  图表（PNG）
│   │   ├── wing_analysis.png     几何与弦长分布（analyze_dxf.py）
│   │   ├── wing_shape.png        翅膀与转轴平面形状（plot_wing_shape.py）
│   │   ├── force_vs_phi.png      力随转角变化（dynamic_analysis.py）
│   │   ├── force_time_domain.png 时间域力曲线（dynamic_analysis.py）
│   │   ├── wing_acceleration.png 角速度/角加速度（dynamic_analysis.py）
│   │   ├── param_scan.png        参数扫描（dynamic_analysis.py）
│   │   ├── alpha_scan.png        安装角扫描（alpha_scan.py）
│   │   ├── mechanism_kinematics.png 机构自检图（mechanism.py）
│   │   └── mechanism_analysis.png 机构可视化（mechanism_plot.py @17.5Hz）
│   ├── tables/                   数据表（CSV）
│   │   └── chord_distribution.csv 弦长分布
│   └── reports/                  报告（Markdown）
│       └── 气动分析报告.md
├── docs/                         文档
│   ├── AGENTS.md                 本文件
│   └── 仿生蝴蝶翅膀空气动力学分析文献综述.md
└── .gitignore
```

## Critical Pitfall: DXF Export Only
- **绝对不要用 VBA `GetSketchSegments`**：返回的是全局坐标，且包含 20 m 长的参考构造线，导致数据完全错乱。
- **正确做法**：在 SolidWorks 中激活目标草图 → 文件 → 另存为 → DXF (R2000+) → 选项 → 仅输出激活草图 → 保存。
- 这样得到的 DXF 使用**草图局部坐标**（mm 量级，而非全局坐标 10,000 mm 量级）。

## DXF Parsing
- 使用自定义轻量级 parser（不依赖 ezdxf），解析 SPLINE / LINE / CIRCLE。
- SPLINE 用 scipy.interpolate.BSpline 采样为密集点（默认 2000 点）。
- 多个 SPLINE/LINE 段通过 nearest-neighbor 首尾相连（tolerance 0.1 mm）。
- **注意**：2026-05-29 修复了 parser 在遇到 code 0 时未终止当前实体块的 bug，避免 knots 数组被后续实体数据污染。

## Coordinate System
- 转轴由 `WingsAxis.DXF` 中两个圆的圆心定义：`p0` 和 `p1`。
- `unit_dir = (p1-p0)/|p1-p0|`：沿转轴方向（展向近似）。
- `unit_perp = (-unit_dir[1], unit_dir[0])`：垂直于转轴方向（弦向近似）。
- 局部坐标：
  - `r = (pts - p0) @ unit_dir`：沿转轴投影（弦向坐标）
  - `y = (pts - p0) @ unit_perp`：垂直转轴投影（展向坐标）
- 展长 `R = y_max - y_min`，面积通过沿展向分条积分弦长得到。

## Geometry Parameters (最新结果，2026-05-29)
| 参数 | 前翅 Front | 后翅 Back |
|------|-----------|-----------|
| 面积 S (mm²) | 16,166.6 | 15,537.6 |
| 展长 R (mm) | 154.3 | 147.4 |
| 平均弦长 c_avg (mm) | 104.8 | 105.4 |
| 展弦比 AR | 1.47 | 1.40 |
| 二阶面积矩 r̂₂² | 0.2382 | 0.2876 |
| 一阶面积矩 r̂₁ | 0.4227 | 0.4798 |

> **注意**：轴线 DXF 在 2026-05-29 导出后，第二个圆心变为 (41.55, 44.73)，导致轴线长度 140.84 mm。前一次导出时第二个圆心为 (0, 0)，长度 86 mm。几何结果随之变化。当前使用最新 DXF 数据。

## Aerodynamic Model
基于文献综述的准定常分解（Dickinson 2001 / Sane & Dickinson 2002）。

### 1. 运动学参数
- **由前置连杆机构（mechanism.py）生成**：曲柄摇杆机构，曲柄一周=翅膀一拍
- 典型频率 `f = 17.5 Hz`（范围 15–20 Hz），下拍 80° 上拍 60°
- 可调参数：主点圆心 x 坐标 `a`（6-14，控制摆幅）和频率 `f`
- 固定参数：`b=6.97`, `R=2.25`, `c=14`, `l=8`
- 当前 (a=8.0, f=17.5Hz): `φ̇_max = 148 rad/s`, `φ̈_max = 20,388 rad/s²`

### a 扫描表 (f=17.5 Hz, φ_down=80° φ_up=60°)
| a | 摆幅 | φ̇_max | φ̈_max | 有效帧 |
|---|------|--------|--------|--------|
| 6 | 35.6° | 159 | 21,966 | 全有效 |
| **8** | **33.3°** | **148** | **20,388** | **全有效** |
| 10 | 32.8° | 137 | 18,776 | 全有效 |
| 12 | 36.8° | 146 | 19,577 | 全有效 |
| 13 | 48.6° | — | — | 312/360 |

> a 越过 12 后出现交点缺失，机构不稳定。

### 2. 气动力系数
- 升力系数：`C_L(α) = 0.255 + 1.58·sin(2.13·α - 7.2°)`，峰值 ~1.83 @ α≈45°
- 阻力系数：`C_D(α) = 1.92 - 1.55·cos(2.04·α - 9.82°)`，峰值 ~3.47 @ α≈90°
- ~~旋转力系数：`C_rot = 1.5`~~（固定攻角，旋转力=0）

### 3. 力分解公式
| 分量 | 峰值公式 | 时均估算 |
|------|---------|---------|
| 平动升力 | `0.5·ρ·C_L·(φ̇·R)²·S·r̂₂²` | 峰值 / 2（cos² 平均） |
| 平动阻力 | `0.5·ρ·C_D·(φ̇·R)²·S·r̂₂²` | 峰值 / 2 |
| ~~旋转力~~ | ~~`ρ·C_rot·α̇·φ̇·c_avg²·R²·r̂₁`~~ | ~~峰值 × 2·flip_ratio~~ |
| 附加质量力 | `-(ρ·π·c_avg²/4)·φ̈·R·r̂₁·sin(α)` | ≈0（周期对称） |

> **重要假设**：
> 1. 翅膀根部到转轴有约 9 mm 间距，但力公式中仍使用 `R = y_max - y_min` 作为有效展长（简化处理，误差约 10–20%）。
> 2. 运动学由前置连杆机构（mechanism.py）生成，曲柄一周=翅膀一拍。
> 3. **固定攻角假设**：机械蝴蝶的翅膀通过刚性结构安装，安装角度固定为 `α = +45°`，扑动过程中**无法主动翻转攻角**。这与真实昆虫的主动扭转机制不同。
> 4. **上拍升力方向反转**：由于攻角固定，平动升力方向由 φ̇ 符号决定。φ̇ ≤ 0（向下）→ 攻角 +α → C_L > 0；φ̇ > 0（向上）→ 攻角 -α → C_L < 0。
> 5. **旋转力消失**：固定攻角意味着 `α̇ = 0`，旋转力项为零。
> 6. 附加质量力：`F_AM = -(ρπc²/4)·φ̈·R·r̂₁·sin(α)`，阻力加速度方向。峰值出现在 stroke reversal（|φ̈|最大），时均≈0（对称抵消）。
> 7. 实际飞行中三维效应、涡脱落、柔性变形会使真实力显著低于准定常估算（可能低 30–50%）。

### 4. 力计算结果（机构运动学，b=6.97, R=2.25, a=8.0）

**静态估算**（`analyze_dxf.py`）：
| 项目 | 四翅总计 |
|------|---------|
| 重量 | 245.25 mN |
| 峰值力 | 34,491 mN |
| 时均升力 | 5,001 mN |
| 时均阻力 | 4,645 mN |
| 升重比 | 20.4 |

**动态分析**（`dynamic_analysis.py`）：
| 项目 | 四翅总计 |
|------|---------|
| 重量 | 245.25 mN |
| 峰值升力 | 41,336 mN |
| 时均升力 | 20,462 mN |
| 时均阻力 | 4,153 mN |
| 升重比 | 83.4 |

> **为什么静态估算（5,001 mN）远低于动态分析（20,462 mN）？**
> - 静态估算只做简化 `峰值/2`，假设半个周期为正升力、半个周期为零；
> - 动态分析完整积分：下拍产生强正升力，上拍产生负升力（向下），但下拍幅度更大（80° vs 60°），净结果仍为正；
> - 静态估算未反映上拍负升力的抵消效应，也未计入附加质量力的时均贡献（周期对称，≈0）。
>
> **注意**：固定攻角模型的时均升力仍远高于重量，原因包括：
> 1. 下拍幅度（80°）大于上拍（60°），下拍产生正升力的时间/幅度占优；
> 2. 准定常模型未计入三维效应和涡脱落造成的升力损失。
>
> 升重比远高于 1，说明在理论模型下升力极为充裕。实际中真实升重比会显著降低。对于机械原理作业，可作为设计裕度的理论上限参考。

## Design Parameters Table
| 参数 | 值 | 来源/假设 |
|------|-----|----------|
| 总质量 m_total | 25 g | 用户给定 |
| 四翅总质量 m_wing_total | 4 g | 用户给定（单翅 1 g） |
| 扑动频率 f | 17.5 Hz | 用户范围 15–20 Hz，取典型值 |
| 下拍幅度 φ_down | 80° | 与水平方向夹角（向下为正） |
| 上拍幅度 φ_up | 60° | 与水平方向夹角（向上为正） |
| 攻角 α | 45° | 固定安装角（刚性结构，无翻转） |
| ~~旋转力系数 C_rot~~ | N/A | 固定攻角 → α̇=0，旋转力=0 |
| 空气密度 ρ | 1.225 kg/m³ | 标准海平面 |
| 运动粘度 ν | 1.46e-5 m²/s | 标准海平面 |
| 机构参数 a | 8.0（可变 6-14） | 主点圆心 x，控制摆幅 |
| 机构参数 b | 6.97（固定） | 主点圆心 y |
| 机构参数 R | 2.25（固定） | 主点轨迹圆半径 |
| 机构参数 c | 14（固定） | 直线方程常数 |
| 机构参数 l | 8（固定） | 固定圆半径 |

## Dynamic Analysis (`dynamic_analysis.py`)
新增功能（2026-05-29）：
1. **机构运动学**：由 `mechanism.py` 生成 φ(t)。可调参数 a（6-14），b/R/c/l 固定。
2. **固定攻角模型**：刚性安装，攻角 +45° 不变，α̇=0，旋转力=0。
3. **C_L 基于瞬时速度方向**：φ̇ ≤ 0 → C_L(+α)；φ̇ > 0 → C_L(-α)。
4. **F_AM = -(ρπc²/4)·φ̈·R·r̂₁·sin(α)**：阻力加速度。
5. **参数扫描**：频率 10-25 Hz、幅度 40-100°、攻角 20-70°。
6. **图表**：`force_vs_phi.png`、`force_time_domain.png`、`wing_acceleration.png`、`param_scan.png`。
7. **Markdown 报告**：`气动分析报告.md`（含机构参数表、α扫描结果）。

## `mechanism_plot.py`
机构运动学可视化，融合原 analyze_matlab_constraint + plot_phi_t_various_R：
- 左上：机构几何轨迹（纯几何，与频率无关）
- 右上下：翅膀 φ/φ̇/φ̈ @ 实际频率（通过 wing_kinematics 生成）
- 左下：a 扫描 φ-t 叠加 @ 实际频率
- 右下：span vs a

用法：`python src/mechanism_plot.py --freq 17.5 --a 8 --a-list 6 8 10 12`

## `alpha_scan.py`
安装角 α 扫描（5-85°），计算净升力（符号平均）和升阻比：

| α | 净升力 | 阻力 | L/D | vs 重量 | 说明 |
|---|--------|------|-----|---------|------|
| 17° | 286 mN | 1,391 | **0.206** | 1.2x | 最佳效率 |
| **45°** | **719 mN** | 4,140 | 0.174 | **2.9x** | **当前设计** |
| 75° | 960 mN | 6,934 | 0.138 | 3.9x | 最大净升力 |

### 转角约定
- 机构运动学：曲柄一周 = 翅膀一拍，φ=0 居中
- `φ < 0`：下拍（翅膀向下运动）
- `φ > 0`：上拍（翅膀向上运动）
- 固定攻角 α = +45°，无翻转

> **重要**：早期 VBA 导出的 `wing_axis.csv`、`wing_back.csv`、`wing_front.csv` 已废弃（全局坐标，含 20m 构造线），已重命名为 `_old`。所有分析现在严格基于 DXF 文件。

## Git
- `.gitignore` 已配置，忽略 `*.png`, `*.json`, `*.csv`, Python cache, SolidWorks 临时文件。
- 但 `气动分析报告.md` 和 `wing_analysis_results.json` 建议保留（分析结果）。
